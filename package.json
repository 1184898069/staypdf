{
  "name": "staypdf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PDF tools that never leave your browser",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-pages.js",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}

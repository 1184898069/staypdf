{
  "name": "staypdf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PDF tools with in-memory server processing",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-pages.js",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
